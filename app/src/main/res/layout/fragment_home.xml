<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- Encabezado superior (logo y fondo naranja) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="#FFA857"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvLogo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Juicy"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:textSize="32sp"/>
    </LinearLayout>

    <!-- Bienvenida + usuario + icono -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="12dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvBienvenida"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="¡Hola! Bienvenido(a)"
                android:textColor="#333333"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/tvUsuario"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Kerlyn Medalith"
                android:textColor="#222222"
                android:textSize="22sp"
                android:textStyle="bold"/>
        </LinearLayout>
        <ImageButton
            android:id="@+id/ivPerfil"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_persona"
            android:background="@drawable/boton_perfil"
            android:scaleType="center"
            android:padding="8dp"/>
    </LinearLayout>

    <!-- barra completar búsqueda -->
    <EditText
        android:id="@+id/etBuscar"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:hint="Buscar"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:textSize="16sp"
        android:background="#F7F7FA"
        android:layout_marginHorizontal="12dp"
        android:layout_marginBottom="8dp"/>

    <!-- Categorías -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="12dp">

        <Button
            android:id="@+id/btnBebidas"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Bebidas"
            android:backgroundTint="#FFF48C"
            android:textColor="#333333"
            android:elevation="0dp"/>

        <Button
            android:id="@+id/btnAlimentos"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Alimentos"
            android:backgroundTint="#FFF48C"
            android:textColor="#333333"
            android:elevation="0dp"
            android:layout_marginStart="12dp"/>
    </LinearLayout>

    <!-- Continúa tu lógica con RecyclerView igual que antes -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerProductos"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingTop="8dp"/>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/colorPrimary"
        app:menu="@menu/menu_inferior"
        app:itemIconTint="@color/white"
        app:itemTextColor="@color/white"/>
</LinearLayout>
